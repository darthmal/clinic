export const environment = {
  production: true,
  apiUrl: '/api/v1' // Backend API URL for production (relative path assumes same domain)
  // Or use an absolute URL: 'https://your-production-domain.com/api/v1'
};